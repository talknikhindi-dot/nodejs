export interface User {
  id: number;
  username: string;
  email: string;
  created_at: Date;
}

// This is where you would define your database schema or ORM models
// For raw SQL, this serves as a type definition
